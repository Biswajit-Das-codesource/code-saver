<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Project</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
           
            background: black;
            color: #fff;
            font-family: Arial, sans-serif;
        }
        .modal-container {
            max-width: 900px;
            margin: 10px auto;
            background-color: #000000;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            border-radius:1.2rem;
            /* border: 1px solid white; */
        }
        .btn-custom {
            background-color: #ffffff;
            color: #000;
            font-weight: bold;
        }
        .btn-custom:hover {
            background-color: #e2e2e2;
            color: black;
        }
        textarea{
            height: 18rem;
            background-color:#353333;
        }
      
    </style>
</head>
<body>
    <div class="modal-container">
        <h2 class="mb-3"><strong>Deploy Your Own Code Privately</strong></h2>
        <form method="post" action="/pvtpost">
            <!-- Name Input -->

            <div class="mb-3">
                <label for="projectName" class="form-label">Enter Your Programming Language</label>
                <input type="text" id="projectName" class="form-control" placeholder="Python,java,JavaScript,C,C++" name="language">
            </div>

            <div class="mb-3">
                <label for="projectName" class="form-label">Title of the code</label>
                <input type="text" id="projectName" class="form-control" placeholder="Enter your Title" name="title">
            </div>
        

            <div class="mb-3">
                <label for="projectName" class="form-label">Paste your Code Here !</label>
                <textarea type="text" id="projectName" class="form-control" placeholder="paste here ....." name="code"></textarea>
            </div>
        
        
            <!-- Framework Selection -->
           
            
            <!-- Action Buttons -->
            <div class="d-flex justify-content-between">
        
                <button type="submit" class="btn btn-custom" style="padding: 0.6rem 3rem;">Deploy</button>
            </div>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>